int disp_stdarg(char *s, ...)
{
	displays the arguments followed by ’\n’, in the order in which they were passed, according to
the value of s, which is composed of the letters c (for char), s (for char*) and i (for int).

}
*/
int main(void)
{
    char[] = "ok";
    
    printf("%d", "ok");
    return 0;
}
