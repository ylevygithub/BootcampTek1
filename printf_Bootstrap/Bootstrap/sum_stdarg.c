int sum_stdarg(int i, int nb, ...)
{
    int size;
    
    if (i = 0)
    {
        if (nb
        return (a + b + c);
    }
    if (i = 1)
    {
        return (the sum of the size of the last nb char strings passed
                passed as parameter);
    }
}
